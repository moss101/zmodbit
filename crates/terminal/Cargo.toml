[package]
name = "modbit-terminal"
version.workspace = true
edition.workspace = true

[dependencies]
portable-pty = "0.9"
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
base64 = "0.22"

[dev-dependencies]
modbit-git = { path = "../git" }
uuid = { workspace = true }

[lints]
workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
# Phase 6 item 2 (docs/36 admission): Landlock LSM bindings (MIT-OR-Apache-2.0)
# for the Linux FS sandbox; pure Rust, no system libs.
landlock = "0.4"
