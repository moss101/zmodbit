// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v7.36.0
// source: modbit/protocol/v1/commands.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { SchemaVersion } from "./common";

export const protobufPackage = "modbit.protocol.v1";

/**
 * Canonical command envelope (docs/30 § Canonical command envelope).
 * Mutating commands are idempotent by command_id; mutating commands carry
 * expected_generation for aggregate-level optimistic concurrency.
 */
export interface CommandEnvelope {
  commandId: string;
  tenantId: string;
  userId: string;
  sessionId?: string | undefined;
  aggregateId?: string | undefined;
  expectedGeneration?: string | undefined;
  commandType: string;
  schemaVersion: SchemaVersion | undefined;
  payload: Uint8Array;
  issuedAt: Date | undefined;
}

export interface CreateSessionCommand {
  displayName: string;
}

export interface CreateTaskCommand {
  sessionId: string;
  title: string;
  prompt: string;
}

export interface CancelTaskCommand {
  taskId: string;
  reason: string;
}

function createBaseCommandEnvelope(): CommandEnvelope {
  return {
    commandId: "",
    tenantId: "",
    userId: "",
    sessionId: undefined,
    aggregateId: undefined,
    expectedGeneration: undefined,
    commandType: "",
    schemaVersion: undefined,
    payload: new Uint8Array(0),
    issuedAt: undefined,
  };
}

export const CommandEnvelope: MessageFns<CommandEnvelope> = {
  encode(message: CommandEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandId !== "") {
      writer.uint32(10).string(message.commandId);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    if (message.userId !== "") {
      writer.uint32(26).string(message.userId);
    }
    if (message.sessionId !== undefined) {
      writer.uint32(34).string(message.sessionId);
    }
    if (message.aggregateId !== undefined) {
      writer.uint32(42).string(message.aggregateId);
    }
    if (message.expectedGeneration !== undefined) {
      writer.uint32(48).uint64(message.expectedGeneration);
    }
    if (message.commandType !== "") {
      writer.uint32(58).string(message.commandType);
    }
    if (message.schemaVersion !== undefined) {
      SchemaVersion.encode(message.schemaVersion, writer.uint32(66).fork()).join();
    }
    if (message.payload.length !== 0) {
      writer.uint32(74).bytes(message.payload);
    }
    if (message.issuedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.issuedAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.aggregateId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expectedGeneration = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.commandType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.schemaVersion = SchemaVersion.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.issuedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandEnvelope {
    return {
      commandId: isSet(object.commandId) ? globalThis.String(object.commandId) : "",
      tenantId: isSet(object.tenantId) ? globalThis.String(object.tenantId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : undefined,
      aggregateId: isSet(object.aggregateId) ? globalThis.String(object.aggregateId) : undefined,
      expectedGeneration: isSet(object.expectedGeneration) ? globalThis.String(object.expectedGeneration) : undefined,
      commandType: isSet(object.commandType) ? globalThis.String(object.commandType) : "",
      schemaVersion: isSet(object.schemaVersion) ? SchemaVersion.fromJSON(object.schemaVersion) : undefined,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      issuedAt: isSet(object.issuedAt) ? fromJsonTimestamp(object.issuedAt) : undefined,
    };
  },

  toJSON(message: CommandEnvelope): unknown {
    const obj: any = {};
    if (message.commandId !== "") {
      obj.commandId = message.commandId;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.sessionId !== undefined) {
      obj.sessionId = message.sessionId;
    }
    if (message.aggregateId !== undefined) {
      obj.aggregateId = message.aggregateId;
    }
    if (message.expectedGeneration !== undefined) {
      obj.expectedGeneration = message.expectedGeneration;
    }
    if (message.commandType !== "") {
      obj.commandType = message.commandType;
    }
    if (message.schemaVersion !== undefined) {
      obj.schemaVersion = SchemaVersion.toJSON(message.schemaVersion);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.issuedAt !== undefined) {
      obj.issuedAt = message.issuedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandEnvelope>, I>>(base?: I): CommandEnvelope {
    return CommandEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandEnvelope>, I>>(object: I): CommandEnvelope {
    const message = createBaseCommandEnvelope();
    message.commandId = object.commandId ?? "";
    message.tenantId = object.tenantId ?? "";
    message.userId = object.userId ?? "";
    message.sessionId = object.sessionId ?? undefined;
    message.aggregateId = object.aggregateId ?? undefined;
    message.expectedGeneration = object.expectedGeneration ?? undefined;
    message.commandType = object.commandType ?? "";
    message.schemaVersion = (object.schemaVersion !== undefined && object.schemaVersion !== null)
      ? SchemaVersion.fromPartial(object.schemaVersion)
      : undefined;
    message.payload = object.payload ?? new Uint8Array(0);
    message.issuedAt = object.issuedAt ?? undefined;
    return message;
  },
};

function createBaseCreateSessionCommand(): CreateSessionCommand {
  return { displayName: "" };
}

export const CreateSessionCommand: MessageFns<CreateSessionCommand> = {
  encode(message: CreateSessionCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.displayName !== "") {
      writer.uint32(10).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSessionCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSessionCommand {
    return { displayName: isSet(object.displayName) ? globalThis.String(object.displayName) : "" };
  },

  toJSON(message: CreateSessionCommand): unknown {
    const obj: any = {};
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSessionCommand>, I>>(base?: I): CreateSessionCommand {
    return CreateSessionCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSessionCommand>, I>>(object: I): CreateSessionCommand {
    const message = createBaseCreateSessionCommand();
    message.displayName = object.displayName ?? "";
    return message;
  },
};

function createBaseCreateTaskCommand(): CreateTaskCommand {
  return { sessionId: "", title: "", prompt: "" };
}

export const CreateTaskCommand: MessageFns<CreateTaskCommand> = {
  encode(message: CreateTaskCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.prompt !== "") {
      writer.uint32(26).string(message.prompt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateTaskCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTaskCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTaskCommand {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
    };
  },

  toJSON(message: CreateTaskCommand): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateTaskCommand>, I>>(base?: I): CreateTaskCommand {
    return CreateTaskCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateTaskCommand>, I>>(object: I): CreateTaskCommand {
    const message = createBaseCreateTaskCommand();
    message.sessionId = object.sessionId ?? "";
    message.title = object.title ?? "";
    message.prompt = object.prompt ?? "";
    return message;
  },
};

function createBaseCancelTaskCommand(): CancelTaskCommand {
  return { taskId: "", reason: "" };
}

export const CancelTaskCommand: MessageFns<CancelTaskCommand> = {
  encode(message: CancelTaskCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelTaskCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelTaskCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelTaskCommand {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: CancelTaskCommand): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelTaskCommand>, I>>(base?: I): CancelTaskCommand {
    return CancelTaskCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelTaskCommand>, I>>(object: I): CancelTaskCommand {
    const message = createBaseCancelTaskCommand();
    message.taskId = object.taskId ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
