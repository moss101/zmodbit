// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v7.36.0
// source: modbit/protocol/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { SchemaVersion } from "./common";
import { TaskStatus, taskStatusFromJSON, taskStatusToJSON } from "./domain";

export const protobufPackage = "modbit.protocol.v1";

/**
 * Canonical event envelope (docs/30 § Canonical event types). Events are the
 * only durable truth; they append to the event store with aggregate generation
 * fencing (MOD-STATE-001/002/003).
 */
export interface EventEnvelope {
  eventId: string;
  tenantId: string;
  aggregateId: string;
  generation: string;
  eventType: string;
  schemaVersion: SchemaVersion | undefined;
  payload: Uint8Array;
  occurredAt: Date | undefined;
}

/**
 * Session/task event family slice (docs/30 § Session/task). Discrete task
 * events ride a typed payload; the full event set accretes with milestones.
 */
export interface TaskEvent {
  taskId: string;
  generation: string;
  taskCreated?: TaskCreated | undefined;
  taskStarted?: TaskStarted | undefined;
  taskCompleted?: TaskCompleted | undefined;
  taskFailed?: TaskFailed | undefined;
}

export interface TaskCreated {
  sessionId: string;
  title: string;
  prompt: string;
  initialStatus: TaskStatus;
}

export interface TaskStarted {
  startedAt: Date | undefined;
}

export interface TaskCompleted {
  summary: string;
}

export interface TaskFailed {
  errorKind: string;
  message: string;
}

function createBaseEventEnvelope(): EventEnvelope {
  return {
    eventId: "",
    tenantId: "",
    aggregateId: "",
    generation: "0",
    eventType: "",
    schemaVersion: undefined,
    payload: new Uint8Array(0),
    occurredAt: undefined,
  };
}

export const EventEnvelope: MessageFns<EventEnvelope> = {
  encode(message: EventEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    if (message.aggregateId !== "") {
      writer.uint32(26).string(message.aggregateId);
    }
    if (message.generation !== "0") {
      writer.uint32(32).uint64(message.generation);
    }
    if (message.eventType !== "") {
      writer.uint32(42).string(message.eventType);
    }
    if (message.schemaVersion !== undefined) {
      SchemaVersion.encode(message.schemaVersion, writer.uint32(50).fork()).join();
    }
    if (message.payload.length !== 0) {
      writer.uint32(58).bytes(message.payload);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.aggregateId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.generation = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.schemaVersion = SchemaVersion.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventEnvelope {
    return {
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : "",
      tenantId: isSet(object.tenantId) ? globalThis.String(object.tenantId) : "",
      aggregateId: isSet(object.aggregateId) ? globalThis.String(object.aggregateId) : "",
      generation: isSet(object.generation) ? globalThis.String(object.generation) : "0",
      eventType: isSet(object.eventType) ? globalThis.String(object.eventType) : "",
      schemaVersion: isSet(object.schemaVersion) ? SchemaVersion.fromJSON(object.schemaVersion) : undefined,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      occurredAt: isSet(object.occurredAt) ? fromJsonTimestamp(object.occurredAt) : undefined,
    };
  },

  toJSON(message: EventEnvelope): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.aggregateId !== "") {
      obj.aggregateId = message.aggregateId;
    }
    if (message.generation !== "0") {
      obj.generation = message.generation;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.schemaVersion !== undefined) {
      obj.schemaVersion = SchemaVersion.toJSON(message.schemaVersion);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventEnvelope>, I>>(base?: I): EventEnvelope {
    return EventEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventEnvelope>, I>>(object: I): EventEnvelope {
    const message = createBaseEventEnvelope();
    message.eventId = object.eventId ?? "";
    message.tenantId = object.tenantId ?? "";
    message.aggregateId = object.aggregateId ?? "";
    message.generation = object.generation ?? "0";
    message.eventType = object.eventType ?? "";
    message.schemaVersion = (object.schemaVersion !== undefined && object.schemaVersion !== null)
      ? SchemaVersion.fromPartial(object.schemaVersion)
      : undefined;
    message.payload = object.payload ?? new Uint8Array(0);
    message.occurredAt = object.occurredAt ?? undefined;
    return message;
  },
};

function createBaseTaskEvent(): TaskEvent {
  return {
    taskId: "",
    generation: "0",
    taskCreated: undefined,
    taskStarted: undefined,
    taskCompleted: undefined,
    taskFailed: undefined,
  };
}

export const TaskEvent: MessageFns<TaskEvent> = {
  encode(message: TaskEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.generation !== "0") {
      writer.uint32(16).uint64(message.generation);
    }
    if (message.taskCreated !== undefined) {
      TaskCreated.encode(message.taskCreated, writer.uint32(82).fork()).join();
    }
    if (message.taskStarted !== undefined) {
      TaskStarted.encode(message.taskStarted, writer.uint32(90).fork()).join();
    }
    if (message.taskCompleted !== undefined) {
      TaskCompleted.encode(message.taskCompleted, writer.uint32(98).fork()).join();
    }
    if (message.taskFailed !== undefined) {
      TaskFailed.encode(message.taskFailed, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.generation = reader.uint64().toString();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.taskCreated = TaskCreated.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.taskStarted = TaskStarted.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.taskCompleted = TaskCompleted.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.taskFailed = TaskFailed.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskEvent {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      generation: isSet(object.generation) ? globalThis.String(object.generation) : "0",
      taskCreated: isSet(object.taskCreated) ? TaskCreated.fromJSON(object.taskCreated) : undefined,
      taskStarted: isSet(object.taskStarted) ? TaskStarted.fromJSON(object.taskStarted) : undefined,
      taskCompleted: isSet(object.taskCompleted) ? TaskCompleted.fromJSON(object.taskCompleted) : undefined,
      taskFailed: isSet(object.taskFailed) ? TaskFailed.fromJSON(object.taskFailed) : undefined,
    };
  },

  toJSON(message: TaskEvent): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.generation !== "0") {
      obj.generation = message.generation;
    }
    if (message.taskCreated !== undefined) {
      obj.taskCreated = TaskCreated.toJSON(message.taskCreated);
    }
    if (message.taskStarted !== undefined) {
      obj.taskStarted = TaskStarted.toJSON(message.taskStarted);
    }
    if (message.taskCompleted !== undefined) {
      obj.taskCompleted = TaskCompleted.toJSON(message.taskCompleted);
    }
    if (message.taskFailed !== undefined) {
      obj.taskFailed = TaskFailed.toJSON(message.taskFailed);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaskEvent>, I>>(base?: I): TaskEvent {
    return TaskEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaskEvent>, I>>(object: I): TaskEvent {
    const message = createBaseTaskEvent();
    message.taskId = object.taskId ?? "";
    message.generation = object.generation ?? "0";
    message.taskCreated = (object.taskCreated !== undefined && object.taskCreated !== null)
      ? TaskCreated.fromPartial(object.taskCreated)
      : undefined;
    message.taskStarted = (object.taskStarted !== undefined && object.taskStarted !== null)
      ? TaskStarted.fromPartial(object.taskStarted)
      : undefined;
    message.taskCompleted = (object.taskCompleted !== undefined && object.taskCompleted !== null)
      ? TaskCompleted.fromPartial(object.taskCompleted)
      : undefined;
    message.taskFailed = (object.taskFailed !== undefined && object.taskFailed !== null)
      ? TaskFailed.fromPartial(object.taskFailed)
      : undefined;
    return message;
  },
};

function createBaseTaskCreated(): TaskCreated {
  return { sessionId: "", title: "", prompt: "", initialStatus: 0 };
}

export const TaskCreated: MessageFns<TaskCreated> = {
  encode(message: TaskCreated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.prompt !== "") {
      writer.uint32(26).string(message.prompt);
    }
    if (message.initialStatus !== 0) {
      writer.uint32(32).int32(message.initialStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskCreated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.initialStatus = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskCreated {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      initialStatus: isSet(object.initialStatus) ? taskStatusFromJSON(object.initialStatus) : 0,
    };
  },

  toJSON(message: TaskCreated): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.initialStatus !== 0) {
      obj.initialStatus = taskStatusToJSON(message.initialStatus);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaskCreated>, I>>(base?: I): TaskCreated {
    return TaskCreated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaskCreated>, I>>(object: I): TaskCreated {
    const message = createBaseTaskCreated();
    message.sessionId = object.sessionId ?? "";
    message.title = object.title ?? "";
    message.prompt = object.prompt ?? "";
    message.initialStatus = object.initialStatus ?? 0;
    return message;
  },
};

function createBaseTaskStarted(): TaskStarted {
  return { startedAt: undefined };
}

export const TaskStarted: MessageFns<TaskStarted> = {
  encode(message: TaskStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskStarted {
    return { startedAt: isSet(object.startedAt) ? fromJsonTimestamp(object.startedAt) : undefined };
  },

  toJSON(message: TaskStarted): unknown {
    const obj: any = {};
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaskStarted>, I>>(base?: I): TaskStarted {
    return TaskStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaskStarted>, I>>(object: I): TaskStarted {
    const message = createBaseTaskStarted();
    message.startedAt = object.startedAt ?? undefined;
    return message;
  },
};

function createBaseTaskCompleted(): TaskCompleted {
  return { summary: "" };
}

export const TaskCompleted: MessageFns<TaskCompleted> = {
  encode(message: TaskCompleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.summary !== "") {
      writer.uint32(10).string(message.summary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskCompleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskCompleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskCompleted {
    return { summary: isSet(object.summary) ? globalThis.String(object.summary) : "" };
  },

  toJSON(message: TaskCompleted): unknown {
    const obj: any = {};
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaskCompleted>, I>>(base?: I): TaskCompleted {
    return TaskCompleted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaskCompleted>, I>>(object: I): TaskCompleted {
    const message = createBaseTaskCompleted();
    message.summary = object.summary ?? "";
    return message;
  },
};

function createBaseTaskFailed(): TaskFailed {
  return { errorKind: "", message: "" };
}

export const TaskFailed: MessageFns<TaskFailed> = {
  encode(message: TaskFailed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.errorKind !== "") {
      writer.uint32(10).string(message.errorKind);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskFailed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskFailed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.errorKind = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaskFailed {
    return {
      errorKind: isSet(object.errorKind) ? globalThis.String(object.errorKind) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: TaskFailed): unknown {
    const obj: any = {};
    if (message.errorKind !== "") {
      obj.errorKind = message.errorKind;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaskFailed>, I>>(base?: I): TaskFailed {
    return TaskFailed.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaskFailed>, I>>(object: I): TaskFailed {
    const message = createBaseTaskFailed();
    message.errorKind = object.errorKind ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
