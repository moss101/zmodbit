// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               <version-normalized>
// source: modbit/protocol/v1/transport.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "modbit.protocol.v1";

/**
 * Boot handshake over the local SurfaceProtocol transport (docs/30 § Local
 * SurfaceProtocol). Core generates a boot-scoped random secret; the desktop
 * main process receives it through the inherited secure channel and proves
 * possession via HMAC — the secret itself never crosses the wire and is never
 * exposed to the renderer.
 */
export interface Challenge {
  serverNonce: Uint8Array;
}

export interface Hello {
  /** HMAC-SHA256(boot_secret, server_nonce || client_nonce || major || minor) */
  proof: Uint8Array;
  clientNonce: Uint8Array;
  major: number;
  minor: number;
}

export interface AuthResult {
  ok: boolean;
  /**
   * Major-version mismatch leaves the connection usable for read-only export
   * while blocking task mutation (docs/30 § Version compatibility).
   */
  readOnly: boolean;
  negotiatedMajor: number;
  negotiatedMinor: number;
  serverVersion: string;
  error: string;
}

function createBaseChallenge(): Challenge {
  return { serverNonce: new Uint8Array(0) };
}

export const Challenge: MessageFns<Challenge> = {
  encode(message: Challenge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverNonce.length !== 0) {
      writer.uint32(10).bytes(message.serverNonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Challenge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChallenge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverNonce = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Challenge {
    return { serverNonce: isSet(object.serverNonce) ? bytesFromBase64(object.serverNonce) : new Uint8Array(0) };
  },

  toJSON(message: Challenge): unknown {
    const obj: any = {};
    if (message.serverNonce.length !== 0) {
      obj.serverNonce = base64FromBytes(message.serverNonce);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Challenge>, I>>(base?: I): Challenge {
    return Challenge.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Challenge>, I>>(object: I): Challenge {
    const message = createBaseChallenge();
    message.serverNonce = object.serverNonce ?? new Uint8Array(0);
    return message;
  },
};

function createBaseHello(): Hello {
  return { proof: new Uint8Array(0), clientNonce: new Uint8Array(0), major: 0, minor: 0 };
}

export const Hello: MessageFns<Hello> = {
  encode(message: Hello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proof.length !== 0) {
      writer.uint32(10).bytes(message.proof);
    }
    if (message.clientNonce.length !== 0) {
      writer.uint32(18).bytes(message.clientNonce);
    }
    if (message.major !== 0) {
      writer.uint32(24).uint32(message.major);
    }
    if (message.minor !== 0) {
      writer.uint32(32).uint32(message.minor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proof = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientNonce = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.major = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.minor = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hello {
    return {
      proof: isSet(object.proof) ? bytesFromBase64(object.proof) : new Uint8Array(0),
      clientNonce: isSet(object.clientNonce) ? bytesFromBase64(object.clientNonce) : new Uint8Array(0),
      major: isSet(object.major) ? globalThis.Number(object.major) : 0,
      minor: isSet(object.minor) ? globalThis.Number(object.minor) : 0,
    };
  },

  toJSON(message: Hello): unknown {
    const obj: any = {};
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    if (message.clientNonce.length !== 0) {
      obj.clientNonce = base64FromBytes(message.clientNonce);
    }
    if (message.major !== 0) {
      obj.major = Math.round(message.major);
    }
    if (message.minor !== 0) {
      obj.minor = Math.round(message.minor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Hello>, I>>(base?: I): Hello {
    return Hello.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Hello>, I>>(object: I): Hello {
    const message = createBaseHello();
    message.proof = object.proof ?? new Uint8Array(0);
    message.clientNonce = object.clientNonce ?? new Uint8Array(0);
    message.major = object.major ?? 0;
    message.minor = object.minor ?? 0;
    return message;
  },
};

function createBaseAuthResult(): AuthResult {
  return { ok: false, readOnly: false, negotiatedMajor: 0, negotiatedMinor: 0, serverVersion: "", error: "" };
}

export const AuthResult: MessageFns<AuthResult> = {
  encode(message: AuthResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.readOnly !== false) {
      writer.uint32(16).bool(message.readOnly);
    }
    if (message.negotiatedMajor !== 0) {
      writer.uint32(24).uint32(message.negotiatedMajor);
    }
    if (message.negotiatedMinor !== 0) {
      writer.uint32(32).uint32(message.negotiatedMinor);
    }
    if (message.serverVersion !== "") {
      writer.uint32(42).string(message.serverVersion);
    }
    if (message.error !== "") {
      writer.uint32(50).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.readOnly = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.negotiatedMajor = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.negotiatedMinor = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serverVersion = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthResult {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      readOnly: isSet(object.readOnly) ? globalThis.Boolean(object.readOnly) : false,
      negotiatedMajor: isSet(object.negotiatedMajor) ? globalThis.Number(object.negotiatedMajor) : 0,
      negotiatedMinor: isSet(object.negotiatedMinor) ? globalThis.Number(object.negotiatedMinor) : 0,
      serverVersion: isSet(object.serverVersion) ? globalThis.String(object.serverVersion) : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: AuthResult): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.readOnly !== false) {
      obj.readOnly = message.readOnly;
    }
    if (message.negotiatedMajor !== 0) {
      obj.negotiatedMajor = Math.round(message.negotiatedMajor);
    }
    if (message.negotiatedMinor !== 0) {
      obj.negotiatedMinor = Math.round(message.negotiatedMinor);
    }
    if (message.serverVersion !== "") {
      obj.serverVersion = message.serverVersion;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthResult>, I>>(base?: I): AuthResult {
    return AuthResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthResult>, I>>(object: I): AuthResult {
    const message = createBaseAuthResult();
    message.ok = object.ok ?? false;
    message.readOnly = object.readOnly ?? false;
    message.negotiatedMajor = object.negotiatedMajor ?? 0;
    message.negotiatedMinor = object.negotiatedMinor ?? 0;
    message.serverVersion = object.serverVersion ?? "";
    message.error = object.error ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
